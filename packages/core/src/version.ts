/** Bump together with the package version. Echoed in every SimResult. */
export const ENGINE_VERSION = "1.2.1";

/**
 * Rendered by every surface that shows results (CLI, MCP, web UI).
 * Distributions and assumptions, never promises.
 */
export const DISCLAIMER =
  "Simulation, not prediction. Results are Monte Carlo distributions under the stated assumptions and the " +
  "encoded ruleset, not financial advice and not a guarantee of passing any challenge. Firms change their " +
  "rules; the firm's own published rules are always authoritative. Check the spec's source citations and their lastVerified dates when present.";
